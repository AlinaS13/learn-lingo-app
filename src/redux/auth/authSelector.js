export const getUser = (state) => state.auth.isAuth;
export const getUserName = (state) => state.auth.name;
export const getToken = (state) => state.auth.token;
export const selectUserId = (state) => state.auth.userId;
